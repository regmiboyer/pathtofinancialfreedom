{
  "name": "investment-planner-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Gateway service: owns cross-strategy shared state (tax settings, inflation rate, compare drawer) and proxies requests to per-strategy microservices.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
